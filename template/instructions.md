# Plugin Name

<!--
Structure is fixed so every plugin's instructions render identically.
Use exactly these four sections, in this order, with these headings.
Write in second person. One idea per step. Aim for a 6th-grade reading level
for middle-school plugins.

Do not include a title style, colors, or HTML. Plain markdown only —
the site renders this with shared styling.
-->

One or two sentences on what the student is about to explore. Lead with the
phenomenon, not the code.

## What you'll do

- The first thing they'll change
- The second thing
- What they should watch happen on the right

## Try it

1. Run the starter code as-is and look at the graph.
2. Change `total_steps` to a bigger number and run it again.
3. Predict what happens before you run it a third time.

## Think about it

- A question with no single right answer, tied to the concept
- A second question that connects the simulation back to the real world

## Functions you can use

| Function               | What it does                              |
| ---------------------- | ----------------------------------------- |
| `plot_series(values)`  | Draws your list of numbers as a line      |
| `set_reference(value)` | Draws a dashed line at a value you expect |
